# Archivos de IDE y sistema operativo
.vscode/ # Configuración de Visual Studio Code
.DS_Store # Archivos de macOS
Thumbs.db # Archivos de Windows

# Archivos temporales y de caché generales
*.tmp
*.log
*.bak
*.swp # Archivos de swap de Vim/Nano
.npm/
.yarn-cache/

# Directorios de módulos y compilación que podrían aparecer en la raíz si hay errores o configs globales
/node_modules/
/.next/
/out/
/build/

# Archivos de configuración de entorno (¡MUY IMPORTANTE PARA LA SEGURIDAD!)
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# Ignorar directorios específicos de subproyectos (¡CRÍTICO para Next.js!)
# Estas líneas son importantes si no hay un .gitignore dentro del subproyecto
clients/*/node_modules/
clients/*/.next/
clients/*/out/
clients/*/build/
# También para el directorio 'web' si existe como subproyecto Next.js
web/node_modules/
web/.next/
web/out/
web/build/

# Dependencias nativas de Node.js que pueden ser grandes o específicas de la plataforma
# Esto es CRÍTICO para next-swc.darwin-arm64.node y otros binarios
*.node
